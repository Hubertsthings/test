{
  "name": "openbare-registry",
  "version": "1.0.0",
  "description": "Registry service for OpenBare - a directory of community bare server nodes",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test 'test/**/*.test.js'",
    "lint": "eslint *.js",
    "init-db": "node -e \"require('./db').initialize()\""
  },
  "keywords": [
    "bare-server",
    "proxy",
    "registry",
    "openbare"
  ],
  "author": "OpenBare Community",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-cron": "^4.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
